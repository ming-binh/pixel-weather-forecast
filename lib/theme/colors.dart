import 'package:flutter/material.dart';

/// Shared color tokens used across every screen, taken directly from the
/// design's repeated inline hex values.
class PixelColors {
  PixelColors._();

  static const ink = Color(0xFF2B2B44);
  static const cream = Color(0xFFFDF6E7);
  static const creamPanelBg = Color(0xFFF6ECD8);
  static const creamPanelActive = Color(0xFFF3E4C6);
  static const accentOrange = Color(0xFFF2A03D);
  static const mutedGray = Color(0xFF6B6A85);
  static const errorPink = Color(0xFFF7DCD2);
  static const errorRed = Color(0xFFC2694A);
  static const skeletonBg = Color(0xFFE9DCC0);
  static const skeletonBar = Color(0xFFD6C7A6);
  static const appBg = Color(0xFF12131C);
}
