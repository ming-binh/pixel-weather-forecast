<?xml version="1.0" encoding="utf-8"?>
<!-- 4x1 cells: 70dp*cells - 30dp, matches WeatherWidgetSnapshot.size in Dart.
     updatePeriodMillis is just a safety net — the real refresh cadence is
     driven by the WorkManager task registered in main.dart, since Android
     ignores update periods under 30 minutes anyway. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="260dp"
    android:minHeight="68dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/weather_widget"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen">
</appwidget-provider>
